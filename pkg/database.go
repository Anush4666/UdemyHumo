package pkg

import "Udemy/pkg/models"

var Users = make([]models.Users, 0)
var Subscription = make([]models.Subscription, 0)
var Region = make([]models.Region, 0)
var Course = make([]models.Course, 0)
var City = make([]models.City, 0)
var Category = make([]models.Category, 0)
